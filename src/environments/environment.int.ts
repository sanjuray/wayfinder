/**
 * Int environment — used by `ng build --configuration=int`.
 * Replace apiBaseUrl once the int backend has a real domain/URL.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'https://int-api.wayfinder.app/api',
};
